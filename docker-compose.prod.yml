name: skeleton-web

services:
  db:
    image: postgres:16-alpine
    container_name: skeleton-web-db
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
      - skeleton_network
    restart: unless-stopped

  backend:
    container_name: skeleton-web-backend
    build:
      context: ./backend
      dockerfile: Dockerfile
    env_file: .env
    depends_on:
      db:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8000/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s
    networks:
      - skeleton_network
    restart: unless-stopped

  frontend:
    container_name: skeleton-web-frontend
    build:
      context: ./frontend
      dockerfile: Dockerfile
      target: prod
    # No ports exposed — let an upstream proxy (nginx, Caddy, Traefik…) handle TLS
    # and routing. Join the proxy's network or add a port mapping here if needed.
    depends_on:
      backend:
        condition: service_healthy
    networks:
      - skeleton_network
    restart: unless-stopped

volumes:
  postgres-data:

networks:
  skeleton_network:
    driver: bridge
