{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'echo \"$CLAUDE_TOOL_INPUT\" | python3 -c \"import sys,json; d=json.load(sys.stdin); path=d.get(\\\"file_path\\\",d.get(\\\"path\\\",\\\"\\\")); exit(0 if \\\"backend/src\\\" in path else 1)\"' && docker compose exec -T backend pytest tests/ -v --tb=short -q 2>&1 | tail -20 || true",
            "timeout": 60
          }
        ]
      }
    ]
  }
}
